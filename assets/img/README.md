# Images

| File | Status |
| --- | --- |
| `incident-channel.svg` | Placeholder mock-up. Replace with a real screenshot of a CMDR incident channel (redact patient-identifiable and staff data first). |
| `incident-report.svg` | Placeholder mock-up. Replace with a real screenshot of a generated root cause report (redact first). |
| `bch-logo.svg` | **Not present.** Needed only if the Founding Hospital Partner block on `index.html` and `about.html` is enabled — which requires written confirmation that Boston Children's Hospital permits name and logo use on the website. |
| `idha-logo.svg` | **Not present.** Same condition, for the BCH Innovation and Digital Health Accelerator. |

The two mock-ups are hand-drawn SVG so the pages are never broken and never
show a grey box, but they are illustrations, not product screenshots. Swap in
real captures before the site goes to a hospital CIO.
